FAQ
===

Q: What input columns are required?
A: ``timestamp``, ``v_bus``, ``i_inc``, and ``p_total`` are required.

Q: Can I run with defaults?
A: Yes. The CLI pipeline supports guided defaults for common workflows.

